# =============================================================================
# DATABASE CONFIGURATION (PostgreSQL)
# =============================================================================

# URL ??????????? ? ?? - ?????????? ?????? ??? JDBC
spring.datasource.url=jdbc:postgresql://127.0.0.1:5432/aicareer_db
spring.datasource.driver-class-name=org.postgresql.Driver

# ??? ???????????? ??
spring.datasource.username=postgres

# ?????? ???????????? ??
spring.datasource.password=KRMSnoQRnx

# ????????? Hibernate/JPA
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

app.logging.level=${APP_LOGGING_LEVEL:INFO}
app.name=${APP_NAME:AiCareer}
app.version=${APP_VERSION:1.0.0}

# =============================================================================
# SERVER SETTINGS
# =============================================================================

server.port=8080
spring.application.name=AI-Career-Navigator

# =============================================================================
# EXTERNAL SERVICES (???? ????? ????????????)
# =============================================================================

# GigaChat API (??????)
# gigachat.api.url=https://gigachat.dev/api
# gigachat.api.key=your_api_key_here

# ?????? ???????? (??????)
# parser.vacancies.delay=5000
# parser.vacancies.max-pages=10